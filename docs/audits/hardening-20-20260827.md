# Hardening 20 dry-run — 2026-08-27

**Scope:** `main` at `ed9bac60d051e7957913c2eba611dff16d32eabf`  
**Repo:** Vite + React + Supabase (Bookflix). No Next.js / Express route tree.  
**Mode:** implementation pass (2026-08-27). Original dry-run findings below are unchanged. See **Fixed vs Deferred** at the end.  
**Skipped:** `node_modules`, lockfiles (`package-lock.json`, `bun.lockb`), build output (`dist`).  
**shadcn/ui:** included when a check matches (size, dead exports, unused modules).

Status values: `proposed` (at least one finding) | `none found`.

---

## 1. Duplicate utilities

**Status:** proposed

| Finding | Locations |
|---|---|
| Identical CORS header objects copied in both edge functions | `supabase/functions/analyze-book/index.ts:3` · `supabase/functions/parse-book/index.ts:3` |
| Book row → `Book` mapper copied (same fields, slightly different `created_at` fallback) | `src/pages/Index.tsx:18` · `src/pages/TitleDetail.tsx:25` · `src/pages/Watch.tsx:19` |
| HTML/XML entity decode + tag-strip pipeline copied | `supabase/functions/parse-book/index.ts:128` (`extractTextFromDocx`) · `supabase/functions/parse-book/index.ts:201` (`extractTextFromEpub`) |
| Genre/category bucketing copied (fiction / non-fiction / business) | `src/pages/Index.tsx:42` · `src/data/mock-books.ts:129` (`getBooksByCategory`) |
| Dynamic `JSZip` import copied | `supabase/functions/parse-book/index.ts:122` · `supabase/functions/parse-book/index.ts:145` |
| Dual toast stacks mounted together | `src/App.tsx:1` (`Toaster`) · `src/App.tsx:2` (`Sonner`) · `src/App.tsx:21` · `src/App.tsx:22` |
| `useToast` re-export wrapper duplicates the real hook module | `src/hooks/use-toast.ts:186` · `src/components/ui/use-toast.ts:1` |
| Edge-function Supabase bootstrap (`createClient` + `SUPABASE_URL` + service role) copied | `supabase/functions/parse-book/index.ts:235` · `supabase/functions/analyze-book/index.ts:256` |

**Count:** 8

---

## 2. Secrets in config

**Status:** proposed

`.env` is **git-tracked** (`git ls-files` lists `.env`) and **not listed in `.gitignore`**. Values omitted (kind only).

| File | Line | Kind |
|---|---|---|
| `.env` | 1 | Supabase project id (`VITE_SUPABASE_PROJECT_ID`) |
| `.env` | 2 | Supabase anon / publishable JWT (`VITE_SUPABASE_PUBLISHABLE_KEY`) |
| `.env` | 3 | Supabase project URL (`VITE_SUPABASE_URL`) |

Related (not hardcoded secrets; env *reads*):

| File | Line | Kind |
|---|---|---|
| `src/integrations/supabase/client.ts` | 5 | Reads `VITE_SUPABASE_URL` |
| `src/integrations/supabase/client.ts` | 6 | Reads `VITE_SUPABASE_PUBLISHABLE_KEY` |
| `supabase/config.toml` | 1 | Project id (non-secret identifier) |
| `supabase/functions/parse-book/index.ts` | 235–236 | Reads `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` |
| `supabase/functions/analyze-book/index.ts` | 256–258 | Reads `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `LOVABLE_API_KEY` |

No service-role key or Lovable API key is hardcoded in source.

**Count:** 3 (committed config credential lines)

---

## 3. Functions over 400 lines

**Status:** none found

Largest functions measured (inclusive of signature through closing brace):

| Function | File | Lines | Length |
|---|---|---|---|
| `Deno.serve` callback | `supabase/functions/analyze-book/index.ts` | 242–443 | 202 |
| `Deno.serve` callback | `supabase/functions/parse-book/index.ts` | 221–363 | 143 |
| `ChartTooltipContent` | `src/components/ui/chart.tsx` | 92–227 | 136 |
| `planScenesForChapter` | `supabase/functions/analyze-book/index.ts` | 134–238 | 105 |
| `Sidebar` | `src/components/ui/sidebar.tsx` | 131–218 | 88 |
| `SidebarProvider` | `src/components/ui/sidebar.tsx` | 43–130 | 88 |
| `extractTextFromEpub` | `supabase/functions/parse-book/index.ts` | 144–219 | 76 |
| `analyzeBook` | `supabase/functions/analyze-book/index.ts` | 52–122 | 71 |

`analyze-book/index.ts` is 443 lines as a **file**, but no single function is ≥ 400.

**Count:** 0

---

## 4. Components over 200 lines

**Status:** proposed

Threshold applied to React component **modules** (`.tsx` files that export UI). No app page exceeds 200 (`Upload.tsx` 183, `Watch.tsx` 169, `TitleDetail.tsx` 152). All hits are unused shadcn scaffolds (see check 5).

| File | Lines | Note |
|---|---|---|
| `src/components/ui/sidebar.tsx` | 637 | Multi-export module; no single export ≥ 200 |
| `src/components/ui/chart.tsx` | 303 | Largest export `ChartTooltipContent` is 136 lines |
| `src/components/ui/carousel.tsx` | 224 | Module over 200 |
| `src/components/ui/menubar.tsx` | 207 | Module over 200 |

**Count:** 4

---

## 5. Dead code

**Status:** proposed

### Unused application modules / exports

| Item | File | Line |
|---|---|---|
| `NavLink` never imported | `src/components/NavLink.tsx` | 11 |
| `useIsMobile` only imported by unused sidebar | `src/hooks/use-mobile.tsx` | 5 |
| Re-export unused by app code | `src/components/ui/use-toast.ts` | 1 |
| `Chapter` interface never imported | `src/types/book.ts` | 22 |
| `Scene` interface never imported | `src/types/book.ts` | 31 |
| `current_episode` never read in UI | `src/types/book.ts` | 16 · `src/data/mock-books.ts` | 47 |
| Placeholder test only (`expect(true)`) | `src/test/example.test.ts` | 3 |
| `error` destructured and unused | `src/pages/TitleDetail.tsx` | 19 |
| `currentScene` state never updated (stuck at 0) | `src/pages/Watch.tsx` | 35 |
| Navbar search input never queries data | `src/components/layout/Navbar.tsx` | 33 |
| Report button has no handler | `src/pages/TitleDetail.tsx` | 140 |
| Skip back / skip forward buttons no-op | `src/pages/Watch.tsx` | 133 · 149 |

### Unused shadcn modules (not imported from any live app path)

Live UI imports are only: `button`, `input`, `label`, `checkbox`, `badge`, `progress`, `slider`, `toaster`, `sonner`, `tooltip`, `toast`.

| File | Line |
|---|---|
| `src/components/ui/accordion.tsx` | 1 |
| `src/components/ui/alert-dialog.tsx` | 1 |
| `src/components/ui/alert.tsx` | 1 |
| `src/components/ui/aspect-ratio.tsx` | 1 |
| `src/components/ui/avatar.tsx` | 1 |
| `src/components/ui/breadcrumb.tsx` | 1 |
| `src/components/ui/calendar.tsx` | 1 |
| `src/components/ui/card.tsx` | 1 |
| `src/components/ui/carousel.tsx` | 1 |
| `src/components/ui/chart.tsx` | 1 |
| `src/components/ui/collapsible.tsx` | 1 |
| `src/components/ui/command.tsx` | 1 |
| `src/components/ui/context-menu.tsx` | 1 |
| `src/components/ui/dialog.tsx` | 1 |
| `src/components/ui/drawer.tsx` | 1 |
| `src/components/ui/dropdown-menu.tsx` | 1 |
| `src/components/ui/form.tsx` | 1 |
| `src/components/ui/hover-card.tsx` | 1 |
| `src/components/ui/input-otp.tsx` | 1 |
| `src/components/ui/menubar.tsx` | 1 |
| `src/components/ui/navigation-menu.tsx` | 1 |
| `src/components/ui/pagination.tsx` | 1 |
| `src/components/ui/popover.tsx` | 1 |
| `src/components/ui/radio-group.tsx` | 1 |
| `src/components/ui/resizable.tsx` | 1 |
| `src/components/ui/scroll-area.tsx` | 1 |
| `src/components/ui/select.tsx` | 1 |
| `src/components/ui/separator.tsx` | 1 |
| `src/components/ui/sheet.tsx` | 1 |
| `src/components/ui/sidebar.tsx` | 1 |
| `src/components/ui/skeleton.tsx` | 1 |
| `src/components/ui/switch.tsx` | 1 |
| `src/components/ui/table.tsx` | 1 |
| `src/components/ui/tabs.tsx` | 1 |
| `src/components/ui/textarea.tsx` | 1 |
| `src/components/ui/toggle.tsx` | 1 |
| `src/components/ui/toggle-group.tsx` | 1 |

### Unused declared dependencies (imported only by dead modules, or never)

| Package | Evidence |
|---|---|
| `zod` | no `from "zod"` in `src/` or `supabase/` |
| `date-fns` | no imports |
| `@hookform/resolvers` | no imports |
| `react-hook-form` | only `src/components/ui/form.tsx:4` (dead) |
| `recharts` | only `src/components/ui/chart.tsx:2` (dead) |

**Count:** 54

---

## 6. Silent / empty catch

**Status:** proposed

No empty `catch {}` / `catch { /* noop */ }` blocks. Failures are swallowed or logged-only:

| File | Line | What happens |
|---|---|---|
| `supabase/functions/analyze-book/index.ts` | 395 | `catch (sceneErr)` logs and continues; chapter failure hidden from caller |
| `supabase/functions/analyze-book/index.ts` | 370 | Scene insert error logged; loop continues |
| `supabase/functions/parse-book/index.ts` | 341 | Fire-and-forget `fetch(...).catch` logs only |
| `supabase/functions/parse-book/index.ts` | 343 | Outer trigger `catch` logs only; parse still returns success |
| `src/pages/Upload.tsx` | 81 | `fnErr` logged; user still gets success toast (L86) |
| `src/pages/Watch.tsx` | 17 | `const { data }` — Supabase `error` discarded |
| `src/pages/TitleDetail.tsx` | 19 | `error` unused; query falls through to mock |
| `src/contexts/AuthContext.tsx` | 28 | `getSession().then(...)` has no `.catch` |

**Count:** 8

---

## 7. UI API calls missing loading / error

**Status:** proposed

| File | Line | Loading | Error UI |
|---|---|---|---|
| `src/pages/Index.tsx` | 10 | missing (`useQuery` ignores `isLoading`) | missing (`isError` unused; failed query falls back to `mockBooks` at L36) |
| `src/pages/TitleDetail.tsx` | 15 | present (L47) | missing — DB `error` ignored (L19); mock fallback (L42) looks like a miss |
| `src/pages/Watch.tsx` | 14 | present (L37) | missing — DB `error` discarded (L17); mock fallback (L28) |
| `src/pages/Upload.tsx` | 77 | present for upload | parse invoke failure not shown (L81–86 success path) |
| `src/components/layout/Navbar.tsx` | 60 | `signOut()` has no pending/error UI | missing |

Present (not counted): `src/pages/Auth.tsx:21` / `:26` / `:33` (button disabled + toasts). Upload storage/insert path toasts at `src/pages/Upload.tsx:88`.

**Count:** 5

---

## 8. DB queries in route handlers

**Status:** proposed

No `app/api`, `pages/api`, or Express handlers. Client React Router pages and Deno edge `Deno.serve` handlers query PostgREST / Storage directly.

| Handler | File | Line |
|---|---|---|
| `/` library list | `src/pages/Index.tsx` | 13 |
| `/title/:id` | `src/pages/TitleDetail.tsx` | 19 |
| `/watch/:id` | `src/pages/Watch.tsx` | 17 |
| `/upload` insert + storage | `src/pages/Upload.tsx` | 51 · 60 |
| `parse-book` | `supabase/functions/parse-book/index.ts` | 240 · 254 · 257 · 262 · 304 · 308 · 323 |
| `analyze-book` | `supabase/functions/analyze-book/index.ts` | 266 · 279 · 285 · 292 · 320 · 366 · 388 · 410 |

**Count:** 6

---

## 9. Sync I/O in request handlers

**Status:** none found

No `readFileSync`, `writeFileSync`, `existsSync`, or `Deno.readTextFileSync`. Edge handlers use `await` on Storage / `fetch`. `extractTextFromTxt` / `extractTextFromPdf` (`supabase/functions/parse-book/index.ts:72` / `:76`) are synchronous CPU on an in-memory `Uint8Array`, not filesystem I/O.

**Count:** 0

---

## 10. List endpoints without pagination

**Status:** proposed

| File | Line | Query |
|---|---|---|
| `src/pages/Index.tsx` | 13–16 | `from("books").select("*").order(...)` — no `.range` / `.limit` |
| `supabase/functions/analyze-book/index.ts` | 285–289 | `from("chapters").select("*")` — no pagination |

No other list endpoints. `src/components/ui/pagination.tsx` exists but is unused.

**Count:** 2

---

## 11. Inconsistent API shapes

**Status:** proposed

| Finding | Locations |
|---|---|
| Success bodies use different keys for the same pipeline | `supabase/functions/parse-book/index.ts:347` `{ success, chapters, words, estimated_minutes }` vs `supabase/functions/analyze-book/index.ts:425` `{ success, synopsis, genre, tags, total_scenes, runtime_minutes }` |
| Error bodies are `{ error: string }` in both (OK) but HTTP 400 vs 404 vs 500 usage differs | parse `229` / `247` / `263` / `309` · analyze `250` / `273` / `296` / `438` |
| Client book DTOs disagree on `created_at` | Index `src/pages/Index.tsx:31` (`b.created_at`) vs TitleDetail `src/pages/TitleDetail.tsx:38` / Watch `src/pages/Watch.tsx:25` (`data.created_at \|\| ""`) |
| Query error policy disagrees | Index throws (`src/pages/Index.tsx:17`) vs TitleDetail/Watch swallow (`src/pages/TitleDetail.tsx:19` · `src/pages/Watch.tsx:17`) |
| Status vocabulary disagrees | Type `BookStatus` is `"processing" \| "ready" \| "error"` (`src/types/book.ts:1`) but DB/parser writes `"parsed"` (`supabase/functions/parse-book/index.ts:324`) and Index casts `("parsed" as any)` (`src/pages/Index.tsx:43`) |

**Count:** 5

---

## 12. Floats for money

**Status:** none found

No price / currency / cents / money fields. `REAL` columns are timing only: `chapters.start_time`, `chapters.end_time`, `scenes.duration_seconds` (`supabase/migrations/20260209203634_dcc8a530-b40e-43ba-be84-c9f9ec3bdb25.sql:39`, `:40`, `:64`). `CategoryRow.tsx:27` `amount` is a scroll-pixel distance, not money.

**Count:** 0

---

## 13. Dates as plain strings not ISO 8601

**Status:** proposed

DB `created_at` / `updated_at` are `TIMESTAMPTZ` (ISO when serialized). Client type is an unconstrained `string`. Mock data uses date-only `YYYY-MM-DD` (valid ISO *date*, not a full datetime). Empty-string fallbacks are **not** ISO 8601.

| File | Line | Issue |
|---|---|---|
| `src/types/book.ts` | 18 | `created_at: string` — no ISO constraint |
| `src/pages/TitleDetail.tsx` | 38 | `data.created_at \|\| ""` can yield `""` |
| `src/pages/Watch.tsx` | 25 | `data.created_at \|\| ""` can yield `""` |
| `src/data/mock-books.ts` | 17 | `"2026-02-01"` date-only |
| `src/data/mock-books.ts` | 32 | `"2026-02-03"` date-only |
| `src/data/mock-books.ts` | 49 | `"2026-01-28"` date-only |
| `src/data/mock-books.ts` | 64 | `"2026-02-05"` date-only |
| `src/data/mock-books.ts` | 79 | `"2026-02-06"` date-only |
| `src/data/mock-books.ts` | 94 | `"2026-02-08"` date-only |
| `src/data/mock-books.ts` | 109 | `"2026-02-04"` date-only |
| `src/data/mock-books.ts` | 124 | `"2026-01-30"` date-only |

**Count:** 11

---

## 14. External calls without retry / backoff

**Status:** proposed

No `retry` / `backoff` / `exponential` symbols in app or edge source. React Query default retries apply only to `useQuery` in Index / TitleDetail / Watch.

| File | Line | Call |
|---|---|---|
| `supabase/functions/analyze-book/index.ts` | 30 | `fetch` to `ai.gateway.lovable.dev` — single attempt; 429/5xx throw |
| `supabase/functions/parse-book/index.ts` | 334 | `fetch` to `analyze-book` — fire-and-forget, no retry |
| `src/pages/Upload.tsx` | 77 | `supabase.functions.invoke("parse-book")` — no retry |
| `src/contexts/AuthContext.tsx` | 38 | `signInWithPassword` — no retry |
| `src/contexts/AuthContext.tsx` | 43 | `signUp` — no retry |
| `src/pages/Upload.tsx` | 51 | Storage `upload` — no retry |

`analyze-book/index.ts:404` sleeps 1s between chapters to reduce rate limits; that is pacing, not retry on failure.

**Count:** 6

---

## 15. Stale comments

**Status:** proposed

| File | Line | Why stale |
|---|---|---|
| `src/integrations/supabase/client.ts` | 1 | Says “automatically generated. Do not edit it directly” but file has custom `auth.storage` / `persistSession` / `autoRefreshToken` (L12–16) |
| `src/pages/Upload.tsx` | 83 | “Don't block — parsing runs async” — `invoke` is `await`ed (L77), so the request **does** block until `parse-book` returns |
| `.lovable/plan.md` | 19 | “three core tables plus an enum” — migration creates no enum |
| `.lovable/plan.md` | 42 | “PDF: extract text using pdf-parse” — implementation is regex stream parse (`supabase/functions/parse-book/index.ts:76`) |
| `README.md` | 17 · 25 · 77 | `REPLACE_WITH_PROJECT_ID` leftover Lovable template |

Still accurate (not counted): `src/pages/Watch.tsx:53` “Mock scenes for demo”; `src/pages/TitleDetail.tsx:18` “Try database first”.

**Count:** 5

---

## 16. Unvalidated input

**Status:** proposed

`zod` is a dependency but unused. Validation is truthy / HTML-only / extension-only.

| File | Line | Gap |
|---|---|---|
| `supabase/functions/parse-book/index.ts` | 227 | `book_id` from `req.json()` — truthy only, not UUID |
| `supabase/functions/analyze-book/index.ts` | 248 | same |
| `src/pages/TitleDetail.tsx` | 13 · 22 | `useParams().id` passed to `.eq("id", id!)` with no UUID check |
| `src/pages/Watch.tsx` | 13 · 17 | same |
| `src/pages/Upload.tsx` | 29 | File type by extension only; no MIME / magic-byte check |
| `src/pages/Upload.tsx` | 50 | Raw `file.name` interpolated into storage path — no size cap, no filename sanitize |
| `src/pages/Auth.tsx` | 62 · 80 | Email/password: HTML `required` / `minLength={6}` only |
| `supabase/functions/analyze-book/index.ts` | 121 · 232 | `JSON.parse(call.function.arguments)` — no schema validate of model output |
| `supabase/functions/parse-book/index.ts` | 298 | `ch.text` stored as `raw_text` with no length bound |

**Count:** 9

---

## 17. API routes missing auth

**Status:** proposed

JWT verification is **off** for both functions. Handlers never call `auth.getUser`. Service role bypasses RLS. Anyone who can reach the function URL can process any `book_id`.

| File | Line | Issue |
|---|---|---|
| `supabase/config.toml` | 4 | `[functions.parse-book] verify_jwt = false` |
| `supabase/config.toml` | 7 | `[functions.analyze-book] verify_jwt = false` |
| `supabase/functions/parse-book/index.ts` | 221 | Handler: no `Authorization` check |
| `supabase/functions/analyze-book/index.ts` | 242 | Handler: no `Authorization` check |
| `supabase/functions/analyze-book/index.ts` | 3 | CORS `Access-Control-Allow-Origin: *` |
| `supabase/functions/parse-book/index.ts` | 3 | CORS `Access-Control-Allow-Origin: *` |
| `supabase/migrations/20260209203634_dcc8a530-b40e-43ba-be84-c9f9ec3bdb25.sql` | 26 | `books` SELECT `USING (true)` |
| `supabase/migrations/20260209203634_dcc8a530-b40e-43ba-be84-c9f9ec3bdb25.sql` | 46 | `chapters` SELECT `USING (true)` (includes `raw_text`) |
| `supabase/migrations/20260209203634_dcc8a530-b40e-43ba-be84-c9f9ec3bdb25.sql` | 70 | `scenes` SELECT `USING (true)` |
| `supabase/migrations/20260209203634_dcc8a530-b40e-43ba-be84-c9f9ec3bdb25.sql` | 88 | `book-uploads` bucket `public` |
| `supabase/migrations/20260209203634_dcc8a530-b40e-43ba-be84-c9f9ec3bdb25.sql` | 90 | Anyone can read uploads |
| `supabase/migrations/20260209203634_dcc8a530-b40e-43ba-be84-c9f9ec3bdb25.sql` | 92–93 | Any authenticated user can update/delete any object in `book-uploads` (not owner-scoped) |

Client `/`, `/title/:id`, `/watch/:id` are intentionally public; Upload is client-gated only (`src/pages/Upload.tsx:24`).

**Count:** 12

---

## 18. Missing indexes

**Status:** proposed

Migration defines PKs and FKs only. No `CREATE INDEX`. Query patterns that lack a supporting secondary index:

| File | Line | Column / access |
|---|---|---|
| `supabase/migrations/20260209203634_dcc8a530-b40e-43ba-be84-c9f9ec3bdb25.sql` | 5 | `books.user_id` (FK + RLS) — no index |
| `src/pages/Index.tsx` | 16 | `books.created_at` `ORDER BY` — no index |
| `src/pages/Index.tsx` | 43–47 | `books.status` / `books.genre` filtered in memory after `select("*")` |
| `supabase/functions/analyze-book/index.ts` | 288 | `chapters.book_id` filter + `order` — no `(book_id, order)` index (`sql:34`, `:36`) |
| `supabase/functions/analyze-book/index.ts` | 357 | `scenes.chapter_id` inserts / future lookups — no `(chapter_id, order)` index (`sql:57`, `:58`) |

**Count:** 5

---

## 19. N+1 queries

**Status:** proposed

| File | Line | Pattern |
|---|---|---|
| `supabase/functions/analyze-book/index.ts` | 340 | `for (const chapter of chapters)` — per chapter: AI call, `scenes.insert` (L366), `chapters.update` (L388) |

One books + one chapters fetch, then **N** insert/update pairs. Scene insert error at L369 does not abort the remaining N.

Client pages each issue a single `books` query (not N+1).

**Count:** 1

---

## 20. SDKs initialized in more than one place

**Status:** proposed

| SDK | File | Line |
|---|---|---|
| `@supabase/supabase-js` `createClient` | `src/integrations/supabase/client.ts` | 11 (npm `^2.95.3`) |
| `@supabase/supabase-js` `createClient` | `supabase/functions/parse-book/index.ts` | 237 (esm.sh `2.49.1`) |
| `@supabase/supabase-js` `createClient` | `supabase/functions/analyze-book/index.ts` | 263 (esm.sh `2.49.1`) |
| `jszip` | `supabase/functions/parse-book/index.ts` | 122 |
| `jszip` | `supabase/functions/parse-book/index.ts` | 145 |

`QueryClient` is constructed once (`src/App.tsx:15`). Client vs edge using different Supabase major-minor versions is part of the split init.

**Count:** 2 SDKs (5 init sites)

---

## Summary

| check | status | count |
|---|---|---|
| 1 duplicate utilities | proposed | 8 |
| 2 secrets in config | proposed | 3 |
| 3 functions over 400 lines | none found | 0 |
| 4 components over 200 lines | proposed | 4 |
| 5 dead code | proposed | 54 |
| 6 silent/empty catch | proposed | 8 |
| 7 UI API calls missing loading/error | proposed | 5 |
| 8 DB queries in route handlers | proposed | 6 |
| 9 sync I/O in request handlers | none found | 0 |
| 10 list endpoints without pagination | proposed | 2 |
| 11 inconsistent API shapes | proposed | 5 |
| 12 floats for money | none found | 0 |
| 13 dates as plain strings not ISO 8601 | proposed | 11 |
| 14 external calls without retry/backoff | proposed | 6 |
| 15 stale comments | proposed | 5 |
| 16 unvalidated input | proposed | 9 |
| 17 API routes missing auth | proposed | 12 |
| 18 missing indexes | proposed | 5 |
| 19 N+1 queries | proposed | 1 |
| 20 SDKs initialized in more than one place | proposed | 2 |

**Totals:** 17 proposed · 3 none found · 146 finding rows.

Highest-severity proposed items (not a fix list): committed `.env` (check 2); `verify_jwt = false` plus service-role edge functions with no caller auth (check 17); public `chapters.raw_text` SELECT (check 17); unpaginated public `books` list (check 10).

---

## Fixed vs Deferred

Implementation pass on `cursor/hardening-audit-report-528a`. History was not rewritten. Operator still rotates the leaked anon JWT.

| Check | Item | Outcome | Notes |
|---|---|---|---|
| 2 | Committed `.env` | **Fixed** | `.gitignore` now ignores `.env`; `git rm --cached .env`; `.env.example` has empty placeholders only |
| 17 | `verify_jwt = false` | **Fixed** | `supabase/config.toml` sets `verify_jwt = true` for both functions |
| 17 | Edge handlers had no caller auth | **Fixed** | Both functions require a bearer token, `getUser` for user JWTs, owner check, fail closed (401/403) |
| 17 | `chapters.raw_text` publicly selectable | **Fixed** | Migration revokes table grants; `authenticated` can SELECT metadata columns only; owner-only RLS |
| 17 | Public storage + any-auth update/delete | **Fixed** | `book-uploads` set private; object policies owner-scoped to `{user_id}/...` |
| 17 | CORS `Access-Control-Allow-Origin: *` | **Deferred** | Browser `functions.invoke` needs a wildcard or a known origin; no production origin configured |
| 17 | Public `books` / `scenes` SELECT | **Deferred** | Library and future player are intentionally public catalog reads |
| 10 | Library `books` list unpaginated | **Fixed** | `Index` uses `.limit(LIBRARY_PAGE_SIZE)` (60) |
| 10 | `analyze-book` chapters unpaginated | **Fixed** | `.limit(200)` |
| 16 | `book_id` not UUID | **Fixed** | Shared `isUuid` in both edge functions |
| 16 | Route `id` not validated | **Fixed** | TitleDetail/Watch skip PostgREST unless UUID; mock ids still work |
| 16 | Upload extension-only / unsanitized name | **Fixed** | MIME + 25 MB cap + `sanitizeFileName` |
| 16 | Auth email HTML-only | **Fixed** | Trim + client email pattern (password min stays 6 so existing accounts still sign in) |
| 16 | AI `JSON.parse` unchecked | **Fixed** | Shape checks + parse errors in `analyze-book` |
| 16 | Unbounded `raw_text` insert | **Fixed** | Cap 500k chars per chapter |
| 14 | AI gateway no retry | **Fixed** | `fetchWithRetry` on 429/5xx |
| 14 | Storage upload / parse invoke no retry | **Fixed** | Client `withRetry` on transient errors |
| 14 | Auth sign-in/up retry | **Deferred** | Retrying bad passwords is unsafe (lockout / rate-limit) |
| 14 | Await analyze-book with retry | **Deferred** | Job can exceed parse-book time limit; trigger stays fire-and-forget but now marks `analyze_trigger_failed` on request failure |
| 6 / 7 | Silent parse invoke + missing library errors | **Fixed** | Upload throws on parse error; Index/TitleDetail/Watch show loading + error + retry; AuthContext `getSession` catch; Navbar sign-out error |
| 6 | Per-chapter scene failures | **Fixed** | Counted; book marked `error` if zero scenes |
| 11 | Book mapper / `parsed` status / `created_at` | **Fixed** | Shared `mapBookRow`; `BookStatus` includes `parsed`; ISO dates |
| 13 | Mock + empty-string dates | **Fixed** | Mocks are full ISO; mapper never returns `""` |
| 15 | Stale generated/README/plan comments | **Fixed** | Client comment, Upload comment, README placeholders, plan.md pdf-parse/enum |
| 18 | Missing indexes | **Fixed** | Same hardening migration adds `user_id`, `created_at`, `status`, `(book_id, order)`, `(chapter_id, order)` |
| 1 | Shared CORS / auth / JSZip / book mapper | **Fixed** | Small DRY only (`_shared/*`, `mapBookRow`, one JSZip import) |
| 1 | Dual Toaster+Sonner, unused shadcn kit | **Deferred** | Sweeping UI deletion is not behavior-preserving |
| 4 / 5 | Unused shadcn modules / NavLink | **Deferred** | Scaffold removal is a large unrelated diff |
| 8 | DB queries in pages / edge functions | **Deferred** | No service layer in this app; moving queries is a refactor |
| 19 | analyze-book N+1 chapter loop | **Deferred** | Batching would change AI pacing and failure isolation |
| 20 | Multiple `createClient` sites | **Deferred** | Browser vs Deno runtimes; versions still differ by design |
| 3 / 9 / 12 | Over-400 / sync I/O / money floats | n/a | none found |

**Must still be applied on the hosted project (not done here):** run the new SQL migration; deploy both edge functions with `verify_jwt`; rotate the leaked anon JWT. This PR does not merge, force-push, deploy, or delete history.
